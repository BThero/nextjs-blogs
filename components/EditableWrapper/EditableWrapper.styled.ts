import styled from 'styled-components';

const Edit = styled.button`
  background-color: transparent;
  border: none;
  font-size: smaller;
  margin-left: 0.25em;

  &:hover {
    color: pink;
    transform: scale(1.1);
    cursor: pointer;
  }
`;

export { Edit };
